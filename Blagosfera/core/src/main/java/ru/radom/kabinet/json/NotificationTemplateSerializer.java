package ru.radom.kabinet.json;

/**
 * Created by rkorablin on 13.05.2015.
 */
// TODO Удалить

/*@Component
public class NotificationTemplateSerializer extends AbstractSerializer<NotificationTemplate> {

    @Override
    public JSONObject serializeInternal(NotificationTemplate object) {
        JSONObject jsonObject = new JSONObject();
        jsonObject.put("id", object.getId());
        jsonObject.put("mnemo", object.getMnemo());
        jsonObject.put("subject", object.getSubject());
        jsonObject.put("shortText", object.getShortText());
        return jsonObject;
    }

}*/
